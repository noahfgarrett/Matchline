{
  "//": "Type-checks the preload. Vite emits the runtime artifact (dist/preload.cjs) because a sandboxed preload must be a single bundled CommonJS file, so nothing is emitted here.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "lib": ["ES2023", "DOM"]
  },
  "include": ["**/*.ts", "../shared/**/*.ts"]
}
