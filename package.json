{
  "name": "matchline",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "npm run test:workspaces && npm run test:legacy && npm run test:integration",
    "test:workspaces": "npm test --workspace @matchline/domain && npm test --workspace @matchline/model-schema && npm test --workspace @matchline/tag-anatomy && npm test --workspace @matchline/spreadsheet-import && npm test --workspace @matchline/asset-catalog && npm test --workspace @matchline/system-resolver && npm test --workspace @matchline/mel-export && npm test --workspace @matchline/connectivity-import && npm test --workspace @matchline/identity && npm test --workspace @matchline/electrical-flow && npm test --workspace @matchline/relationship-claims && npm test --workspace @matchline/learned-rules && npm test --workspace @matchline/ssm-compiler && npm test --workspace @matchline/compiler && npm test --workspace @matchline/scheduling && npm test --workspace @matchline/exto-export && npm test --workspace @matchline/project-store",
    "test:legacy": "npm test --prefix packages/legacy-parity",
    "test:integration": "node --test tests/integration/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
