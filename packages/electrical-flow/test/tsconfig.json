{
  "//": "Type-checks the fixtures the .mjs tests import, so an observation list or enrichment map the tests rely on cannot drift out of what the types actually allow.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["*.ts"],
  "references": [{ "path": ".." }]
}
