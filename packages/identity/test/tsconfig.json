{
  "//": "Type-checks the fixtures the .mjs tests import, so an asset list or profile the tests rely on cannot drift out of what the types actually allow.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["*.ts"],
  "references": [{ "path": ".." }]
}
