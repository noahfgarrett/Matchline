{
  "//": "Type-checks the fixtures the .mjs tests import, so a snapshot, an asset row or a P6 activity the tests rely on cannot drift out of what the types actually allow.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["*.ts"],
  "references": [{ "path": ".." }]
}
