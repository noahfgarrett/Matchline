{
  "//": "Type-checks the fixtures the .mjs tests import, so the invented training rows the tests lean on cannot drift out of what the types actually allow.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["*.ts"],
  "references": [{ "path": ".." }]
}
