/** Types for the vendored `src/core/text.js`. Only what the wrapper uses. */
export declare const clean: (value: unknown) => string;
export declare const natCmp: (left: unknown, right: unknown) => number;
