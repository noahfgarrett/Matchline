{
  "//": "Type-checks the fixtures the .mjs tests import, so a resolver config or MEL row the tests rely on cannot drift out of what the domain types actually allow.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["*.ts"],
  "references": [{ "path": ".." }]
}
