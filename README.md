# SSM Compiler

> **Downloading the app? Go to [SSMCompiler-Releases](https://github.com/noahfgarrett/SSMCompiler-Releases/releases/latest).**
> This is the private source repo — the `SSMCompiler.html` in the file list is the committed build artifact,
> not the distribution channel. Versioned releases (which the app's update check polls) live on the public
> releases repo.

Drop in the five contract-standard project documents — MEL, cable schedule, Easy Power export, PMD, P6 schedule — and compile a complete Start-Up System Matrix: the SSM workbook, a backfilled MEL with provenance, the UPN predecessor matrix, and a QA/acceptance scorecard.

Standalone single-file app (`SSMCompiler.html`), zero runtime dependencies, built on a one-time fork of SSManagement's core modules (see `docs/FORK.md` once the fork lands).

- Design spec: [docs/specs/2026-08-03-ssm-compiler-design.md](docs/specs/2026-08-03-ssm-compiler-design.md)
